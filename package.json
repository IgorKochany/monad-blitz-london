{
  "name": "fairride",
  "version": "1.0.0",
  "private": true,
  "description": "FairRide — peer-to-peer ride auctions with on-chain escrow on Monad. Static dApp (single self-contained index.html).",
  "dependencies": {
    "serve": "^14.2.4"
  },
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  }
}
